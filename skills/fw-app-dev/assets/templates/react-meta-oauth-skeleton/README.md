# React Meta OAuth skeleton (Platform 3.0)

UI + OAuth (GitHub example). **DEW 3.0 required** — `@freshworks/dew-components` + `@freshworks/dew-styles`; no Crayons.

```bash
npm install && fdk run
```
