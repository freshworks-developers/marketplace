# React Meta custom iparams

Copy **`config/`** into a Meta React app when the user needs a custom Settings page. Remove existing **`config/iparams.json`** first.

See **`references/react-meta/custom-iparams.md`**.
