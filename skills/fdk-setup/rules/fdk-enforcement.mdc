# FDK Setup Skill - Enforcement Rules

## Scope

This skill handles **FDK installation and management only**. It does NOT create apps.

## How This Skill Works

**User must explicitly invoke commands (Confluence-style primary names):**
- `/fdk-setup-install` (optional `--version X.Y.Z` for FDK **10.x.y** CDN pin)
- `/fdk-setup-status` (optional `--verbose`; inline only)
- `/fdk-setup-upgrade` (optional `--to X.Y.Z` for pinned semver)
- `/fdk-setup-migrate` (FDK 9 + Node 18 → FDK 10 + Node 24.11)
- `/fdk-setup-downgrade` (optional pinned **9.x.y**; else latest 9 line)
- `/fdk-setup-uninstall` (FDK only — no `--all` / no nvm removal)
- `/fdk-setup-troubleshoot` (inline diagnose; **`--fix`** = shell Task)
- `/fdk-setup-use` (workspace **`nvm use`** / **`.nvmrc`** for FDK 10 vs 9 stack; optional **`--write-nvmrc`**; inline only)

**Legacy aliases (same flows):** `/fdk-install`, `/fdk-status`, `/fdk-upgrade`, `/fdk-downgrade`, `/fdk-uninstall`

**What happens:**
1. User types slash command
2. Command file under `commands/fdk-setup-*.md` defines the Task (or inline status)
3. Subagent spawned with full context for mutating operations
4. Operation executes autonomously
5. Reports back results

## Important Limitation

**This skill does NOT automatically check for FDK when users request app creation.**

If user says "create a freshdesk app" without first running `/fdk-setup-install` (or `/fdk-install`), the agent may create files manually without proper FDK setup.

## Recommended Workflow for App Creation

**Before creating any Freshworks app, users should:**

1. **First, install FDK:** `/fdk-setup-install` (or `/fdk-install`)

2. **Then, optionally install app-dev skill** for templates, Crayons, and app-specific slash commands (those live in app-dev, not fdk-setup).

3. **Then proceed with app creation** using `fdk create` where possible.

### Best Practices

✅ **Remind users** to run `/fdk-setup-install` before creating apps  
✅ **Use `fdk create`** instead of creating files manually when possible  
✅ **Verify FDK** with `fdk version` before using FDK commands  

### Known Limitations

⚠️ **This skill cannot automatically enforce FDK installation** — users must invoke a setup command first.  
⚠️ **No automatic prerequisite checking** when only natural language is used — route to this skill when FDK is needed.

## Skill Location

- **This skill**: `skills/fdk-setup/SKILL.md`
- **App dev skill**: `skills/app-dev/SKILL.md` (separate installation)
