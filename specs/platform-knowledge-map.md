# Platform Knowledge Map — alias

**Superseded by:** `ecosystem-map.md` (same directory when installed under `~/.fw-dev-tools/specs/`).

The ecosystem map is the canonical static knowledge core for controller step 3. This file remains for backward compatibility with tests and docs that reference `platform-knowledge-map.md`.

See **`ecosystem-map.md`** for full content.
